import React from 'react'
import { Header } from '../components'

const Main = () => {
  return (
    <main>
      <Header  className="w-screen min-h-screen flex items-center justify-center flex-col bg-primary" />
    </main>
  )
}

export default Main